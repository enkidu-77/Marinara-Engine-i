# ──────────────────────────────────────────────
# Marinara Engine — Docker Compose
# ──────────────────────────────────────────────
services:
  marinara:
    # Use the pre-built image from GHCR
    image: ghcr.io/spicymarinara/marinara-engine:latest
    ports:
      - "${PORT:-7860}:7860"
    volumes:
      - marinara-data:/app/data
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data
    restart: unless-stopped

volumes:
  marinara-data:
